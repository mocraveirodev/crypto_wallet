<div class="p-5 mb-4 bg-body-secondary rounded-3">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold"><%= t('hello') %> <%= @nome %>!</h1>
    <p class="col-md-8 fs-4"><%= t('message.welcome') %></p>
    <hr class="my-4">
    <p><%= t('message.menu') %></p>
    <%= render "menu" %>
  </div>
</div>
